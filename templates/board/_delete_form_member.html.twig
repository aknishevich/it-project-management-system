<a onclick="removeMember(this)"
   data-ajax-params='{"_method": "DELETE", "_token": "{{ csrf_token('delete' ~ board.id ~ member.id) }}"}'
   data-ajax-url="{{ path('member_delete', {id: board.id, memberId: member.id}) }}"
   data-ajax-replace-id="members"
>Remove member</a>
